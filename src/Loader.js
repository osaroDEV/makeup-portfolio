import React from "react";
import "./loader.css";

const Loader = () => {
  return (
    <h1
      style={{
        color: "gold",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        backgroundColor: "#1b1b32",
      }}
    >
      Teez Beauty&nbsp;<span>Glow</span>
    </h1>
  );
};

export default Loader;
